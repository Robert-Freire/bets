flask
pandas
numpy
scipy
aiohttp
catboost
pytest

# understat: NOT pinned here — install manually after the rest:
#   pip install --no-deps understat
# Reason: understat hard-pins aiohttp==3.8.3 in its metadata, which has no
# Python 3.13 wheel and won't compile against 3.13's CPython API. The pin is
# over-conservative — understat works fine with modern aiohttp at runtime.
# Skipping deps avoids the broken pin while keeping the library installed.
